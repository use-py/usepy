---
outline: deep
---

# last
获取列表的最后一个元素。

```python
from usepy import last
```

### 参数

- `array`: 要查询的列表

### 返回值

返回列表的最后一个元素，如果列表为空则返回 `None`。

### 例子

```python
>>> last([1, 2, 3])
3
>>> last([])
None
```