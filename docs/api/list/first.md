---
outline: deep
---

# first
获取列表的第一个元素。

```python
from usepy import first
```

### 参数

- `array`: 要查询的列表

### 返回值

返回列表的第一个元素，如果列表为空则返回 `None`。

### 例子

```python
>>> first([1, 2, 3])
1
>>> first([])
None
```